# A7.3 页面上下文与真实 Hermes 端到端测试报告

测试时间：2026-08-08

测试环境：Expo Web / Google Chrome，390 x 844 移动视口，触控模式

测试账号：`爸爸`（空密码登录，未修改账号或重新 seed）

代码基线：`cd68c99`，叠加本次 A7.3 工作区实现

Agent 配置：`runtimeKind=hermes`，API 已启用 17 个只读工具，Hermes `familyapp` profile 已加载 24 项工具白名单

## A7.4-A 主场景复核

严格按“必须由预期工具和预期卡片完成，发生回落一律失败”的规则计算，本次真实结果为 **6/9**。`summary.passed` 由明细中 `passed === true` 的条目动态汇总，没有手写通过数。

| 场景 | 输入 | 实际工具 | 耗时 | 结果 |
|---|---|---|---:|---|
| 1 | 查看我的待办任务 | `get_member_tasks` | 21.2s | 通过 |
| 2 | 这周家里有什么安排 | `get_family_schedule` | 15.3s | 通过 |
| 3 | 家里还有哪些菜快过期了 | `get_inventory_alerts` | 8.4s | 失败，未调用预期的 `get_inventory_summary` |
| 4 | 购物清单里有什么 | `get_shopping_list` | 5.4s | 通过 |
| 5 | 搜索不辣的家常菜 | `search_recipes`（4 次） | 19.0s | 通过 |
| 6 | 下周点了什么菜 | 未调用工具 | 11.6s | 失败，模型直接生成答案 |
| 7 | 深圳这几天天气怎么样 | `get_weather` | 11.1s | 通过，安全降级 |
| 8 | 我的个人档案 | `get_member_profile` | 12.5s | 通过 |
| 9 | 我明天有什么安排，需要准备什么食材 | 未调用工具 | 8.5s | 失败，未形成多工具联动 |

- 9 个 run 均为 Hermes，`errorCode=null`，回落数为 **0**。
- 7 个 run 产生工具事件，6 个 run 产生结构化卡片；平均响应时间约 **12.6 秒**。
- 天气服务未配置时调用了 `get_weather`，回答只说明服务不可用，没有输出具体温度、降水量或降水概率。
- 独立可见性闸门 run `8590c921-fc73-46c0-a54d-9b88356d34cc` 同时调用 `get_tasks` 和 `get_member_tasks`，证明成员任务工具对 Hermes 可见。

主场景会话：`56e4b6c8-035a-4156-9a25-59486e978d6c`

## 记忆工具复核

| 输入 | 实际工具 | runId | 耗时 | 结果 |
|---|---|---|---:|---|
| 记住我不吃辣 | `remember_preference` | `f3414506-4879-46b4-a89f-cbe3bc50293d` | 29.6s | 通过 |
| 我有什么饮食偏好 | 未调用工具 | `6524a226-394f-4ad0-9b5f-b54863626c4a` | 3.5s | 失败 |

写入 run 为 Hermes、无回落，并产生待确认的个人私有记忆候选。召回回答沿用了同一会话中的前文，没有调用 `recall_preferences`，因此不能算作记忆检索链路通过。

## A7.3 页面上下文

| 场景 | 实际行为 | runId | 耗时 | 结果 |
|---|---|---|---:|---|
| 从菜品详情进入后询问“这道菜需要什么食材” | 调用 `search_recipes`，命中“不辣家常蒸蛋”并回答番茄、鸡蛋 | `65570778-23a6-4f86-bbcf-1dd4482ce2b1` | 15.3s | 通过 |
| 清除上下文后再次询问同一句 | 未调用工具，明确追问具体菜名，没有自行选择家庭资源 | `88c767c6-ebac-408a-a664-b32e423cebf6` | 4.6s | 通过 |

A7.3 专项结果为 **2/2**。两条 run 均为 Hermes、无回落；第一条证明菜品上下文传入并被模型使用，第二条证明清除后不会继续携带上下文，且无上下文时能够诚实追问。

## 自动回归

- API 全量回归：通过，约 71.7 秒。
- Schema 漂移检查：通过，本批无迁移、无结构变化。
- API build：通过。
- Mobile TypeScript：通过。
- Expo lint：通过。
- Playwright Web：`40 passed / 6 skipped`；默认跳过受 `HERMES_LIVE_E2E` 守卫的真实 Hermes 场景。
- `git diff --check`：通过。

截图与结构化明细见本目录 13 张 PNG 和 `E2E-RESULTS.json`。工具事件和结果均按 `runId` 关联，没有用全表聚合推断本轮结果。

## 已知残留

- 390px 视口下，较长的助手纯文本消息仍可能在右侧裁切。本批按任务约定只记录，不修复。
- 截图左下角的 Expo 开发工具按钮会覆盖少量导航区域，这是开发模式测试伪影，不代表发布构建界面。
