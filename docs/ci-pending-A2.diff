--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -37,6 +37,8 @@
         run: corepack pnpm typecheck
       - name: Lint（客户端 + API）
         run: corepack pnpm lint
+      - name: 新客户端构建（Vite）
+        run: corepack pnpm --filter web build
       - name: 端点清单与代码一致
         run: node scripts/api-inventory.mjs --check
       - name: 备份脚本语法
@@ -129,6 +131,15 @@
           DB_USER: family
           DB_PASSWORD: family123
         run: corepack pnpm test:web
+      - name: 运行新客户端 Playwright 回归
+        if: '!cancelled()'
+        env:
+          CI: '1'
+          DB_HOST: 127.0.0.1
+          DB_PORT: '5433'
+          DB_USER: family
+          DB_PASSWORD: family123
+        run: corepack pnpm test:web:next
       - name: 上传失败截图与 trace
         if: failure()
         uses: actions/upload-artifact@v4
@@ -137,6 +148,8 @@
           path: |
             apps/mobile/test-results
             apps/mobile/playwright-report
+            apps/web/test-results
+            apps/web/playwright-report
           retention-days: 7
 
   # 镜像构建：验证 Dockerfile 与工作区包（packages/*）的复制/构建顺序没有坏掉
